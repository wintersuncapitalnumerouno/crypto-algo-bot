import pandas as pd
from strategies.dvol_bb.params import get_default_params
def get_entries_exits(df: pd.DataFrame, params: dict = None):
    raise NotImplementedError("DVOL_BB not yet implemented.")
