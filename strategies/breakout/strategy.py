import pandas as pd
from strategies.breakout.params import get_default_params
def get_entries_exits(df: pd.DataFrame, params: dict = None):
    raise NotImplementedError("Breakout not yet implemented.")
